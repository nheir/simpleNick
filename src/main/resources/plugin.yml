name: SimpleNick
main: io.github.nheir.simplenick.SimpleNick
version: 1.0

commands:
  nick:
    description: Set/unset a nickname
    usage: /nick [nickname]
    permission: simplenick.nick
    permission-message: You don't have <permission>
      
permissions:
  simplenick.nick:
    description: Allow to set or unset nickname
    default: true
